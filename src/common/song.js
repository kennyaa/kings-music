export default class Song {
  constructor({id, singer, name, image, url, desc}) {
    this.id = id
    this.singer = singer
    this.name = name
    this.image = image
    this.url = url
    this.desc = desc
  }
}